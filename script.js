function addTwoNumbers(a, b) {
    a = +a, b = +b;
    return a + b;
}

function subtractTwoNumbers(a, b) {
    a = +a, b = +b;
    return a - b;
}

function multiplyTwoNumbers(a, b) {
    a = +a, b = +b;
    return a * b;
}